spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: validate
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

logging:
  level:
    root: info
#    org.hibernate.orm.jdbc.bind: TRACE
#    org.springframework.jdbc.core: TRACE
app:
  image:
    bucket: /home/andrey/packages/online-shop/images/products