# Платформа автоматизации продаж страховых продуктов


## Основные цели
- Расчет продукта "ДМС при ДТП" ✅
- Партнерское API для продукта "ДМС при ДТП" ✅
- Добавить партнерские API и расчёт для продукта "Защита дохода 2.0"
- Добавить четкую систему обработки ошибок
- Добавить API для построения графиков
- Добавить code-generator для разработки продуктов в домене

## Основные преимущества в сравнении с Jarvis

1. Гексогональная архитектура:
- деление на полностью независимый **доменный модуль**, который содержит бизнес-логику системы, страховых продуктов.
Это даёт возможность изолированного тестирования бизнес-логики приложения, а также возможность переноса
системы на другие платформы на базе других фрейморков.
- деление на **модуль платформы** на базе **Spring Boot**, который содержит адаптивный слой и основной технологический стек.
Благодаря такому делению, можно не беспокоится что правки в этом модуле заденут бизнес-логику, следовательно можно
оптимизировать производительность, добавлять новые тех. фичи, например кэширование, логирование, AOP, реактивный-стек, **AI-модуль** и т.п.,
причем все фичи идут из коробки **Spring Boot**)
- Можно использовать **BPM-движок для автоматизации бизнес-процессов**, тогда бизнес-логику страхового продукта может описывать
аналитик в виде блок-схем, без привлечения разработчика. Одна из реализаций такого движка - **Camunda**(идет из коробки **Spring Boot**)
- Несколько разработчиков могут независимо вести разработку различных модулей и частей системы,
без риска сломать работу какого-либо разрабатываемого модуля другого коллеги-разработчика.

2. Скорость запуска приложения даже на начальном этапе разработки в несколько раз быстрее, что дает прямую возможность
использования модулей в качестве микросервисов.
3. Готовый веб-движок, что избавляет от необходимости поддерживать свой собственный
4. Удобная и глубокая система конфигурирования в декларативном стиле(что большой плюс). От настроек пула потоков для обработки запросов веб-сервера, 
до настроек страхового продукта, без пересборки и деплоя системы.
Пример:
```yaml
server:
  port: 8081

products:
  dms:
    enable: true
    payment-system:
      name: sber
      enable: true
      url: https://payment.sberbank.ru/partner/123
  nsg:
    enable: true
    pep:
      enable: true
      pep-url: https://digital-signature.com/signature
      retry: 2
    
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true
        hbm2ddl.auto: none
    open-in-view: false
    show-sql: false
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  jackson:
    default-property-inclusion: non_null


logging:
  level:
    org.springframework: INFO
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    com.admiral.onlineshop: DEBUG
    liquibase: INFO
#    org.hibernate.SQL: DEBUG
#    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
```

5. Отсутствие легаси и лишних неиспользуемых модулей, которые не требуются.